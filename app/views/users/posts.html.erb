<!-- app/views/users/posts.html.erb -->
<h1><%= @user.name %> Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, user_post_path(@user, post) %></li>
  <% end %>
</ul>

<%= link_to "Posts with Comments", user_posts_with_comments_path(@user), class: "btn btn-primary" %>
